version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run build
run:
  command: npm start
  network:
    port: 3000
  env:
    - name: NEXT_PUBLIC_API_URL
      value: "https://your-backend-url.awsapprunner.com"
    - name: HOSTNAME
      value: "0.0.0.0"