# ABHA-Sync Next.js MVP

Minimal Next.js MVP for ABHA-Sync (UI-only). Uses Tailwind and Lucide icons.

Quick start:

1. cd next-app
2. npm install
3. npm run dev

API mocks are available under `/pages/api` for FHIR conversion and drug mapping.
