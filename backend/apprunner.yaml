version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m pip install --upgrade pip
run:
  pre-run:
    - pip3 install -r requirements.txt
  command: uvicorn main:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 3000
  network:
    port: 8000